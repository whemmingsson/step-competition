export interface Badge {
  iconUrl: string; // URL to the badge icon image
  type: string; // Type or category of the badge
  description: string; // Description of the badge
  steps: number; // Number of steps required to earn the badge
}
